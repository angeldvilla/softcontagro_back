/* const { Router } = require("express");
const paymentRoutes = Router();
const {
  getProducts,
  createProduct,
  editProduct,
  deleteProduct,
} = require("../controllers/paymentController");

paymentRoutes.get("/", getProducts);

paymentRoutes.post("/add", createProduct);

paymentRoutes.put("/:id", editProduct);

paymentRoutes.delete("/:id", deleteProduct);

module.exports = paymentRoutes; */
